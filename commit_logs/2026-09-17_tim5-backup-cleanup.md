# 配置 TIM5 计时分频并移除冗余备份目录

- 日期：2026-09-17
- 作者：wulingfeng（开发者修改，Codex 协助整理提交）
- 开发基线：d038661
- 状态：配置已保存；同步功能尚未接入。

## 修改目的与内容

- Core/Src/main.c 中 TIM5 Prescaler 从 0 改为 99，ranging_bsp.ioc 同步保存该参数，作为后续时钟同步的计时资源准备。
- 当前 HCLK 为 200 MHz，APB1 分频为 4，按当前定时器时钟配置 TIM5 输入为 100 MHz；除以 PSC+1=100 后为 1 MHz，即每计数 1 us。
- 保留现有向上计数和 0xFFFFFFFF 自动重装值，没有新增时钟同步协议或时间戳接口。
- 删除 grid-backup-20260916-213302、mic-backup-20260916-215114 中共 6 个历史备份文件。Git 已保存历史版本，无需再在工作树中保存整份源码备份。
- IAR settings 下的个人会话、调试配置不纳入此次提交。

## 功能边界与验证

- 已核对 main.c 与 .ioc 的分频设置一致。
- 当前默认 LCD_GRID_DEMO_ONLY=1，MX_TIM5_Init 位于未执行的通用初始化分支；本次配置提交不表示 TIM5 已经开始运行。
- 后续使用时需要明确调用初始化与启动接口，并验证计数、回绕和统一时钟映射。修改系统/APB 时钟后须重新核算计数频率。
- 本次未新增源码逻辑，未重新执行烧录或定时精度测量；此前波形功能上板验证不能代替 TIM5 验证。

## 恢复与交接

备份目录中的文件可从本次提交的父版本或原始历史提交恢复；源码主体不受目录清理影响。团队后续以 Git 记录作为版本备份，临时备份不再随功能提交上传。
