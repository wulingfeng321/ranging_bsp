# 分布式声波测距：范围、方案与开发流程

更新：2026-09-18。代码基线：`89abe36`。依据：《嵌入式系统课程设计—分布式声波测距系统—设计任务书（V1.0）》共 5 页，以及当前源码、Git 历史和开发者上板反馈。

本文是开发计划，不是完成证明。任务书指标、已有实现和建议设计分开记录；未测量的精度不写成已达标。构建及提交规范见 [README](README.md)。

## 1. 已确认范围

- 必做：基础 1–5；提高 2（单点标定）、4（测量质量指示）、6（同步波形显示）。对应 60+5+5+10=80 分的任务范围。
- 有余力再做：提高 5（10 m 量程扩展），更复杂的方向/角度估计。后者在本版任务书中没有单列的提高项目，可作为附加功能讨论，不预先承诺加分。
- 不列入当前主线：提高 1（自动测温）、提高 3（击掌测距），第三板定位等附加功能。
- 2026-09-18 已向开发者确认：基础中的 A/B 侧判断保留，不能随复杂方向估计一起推迟。“拓展 5”按提高第 5 项理解；任务书附加部分仅有一条开放性要求。

### 任务指标对照


| 项目 / 页码       | 验收要求摘要                                                                                                                                                                | 当前状态                                   |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| 基础 1 / p2       | 经交换机上电自动通信；30 s 内同步；GPIO 整秒脉冲宽度至少 1 μs，收敛后连续 10 次上升沿差最大不超过 ±10 μs；可观察角色、收敛状态、偏差                                     | ping 已验证；同步和脉冲未实现              |
| 基础 2 / p2       | 标准音频到达时刻以本地计时基准输出；重复播放稳定性指标 ±100 μs                                                                                                            | 只有观察用候选检测，没有精确到达时间       |
| 基础 3 / p3       | 无计算机辅助测量；播放后 2 s 内两屏显示距离和 A/B 方向；连续自动工作，刷新间隔允许不小于 1 s；0.20–2.00 m 单次误差 ≤±10 mm，至少 10 次标准差 ≤2 mm，同事件两屏差 ≤1 mm | 只有 UI 接口，尚无测距闭环                 |
| 基础 4 / p3       | 普通背景静默 60 s 无结果；换播放设备无需改程序，误差 ≤±15 mm；起始瞬态干扰后自动恢复                                                                                      | 待正式检测器和实验验证                     |
| 基础 5 / p3       | 现场设温，修正声速；LCD 显示温度及声速；温度变更后的距离变化符合规律，允许 ±2 mm 偏差                                                                                      | 未实现                                     |
| 提高 2 / p3       | 在已知距离（如 0.500 m）单点标定一次；0.20–5.00 m 单次误差 ≤±5 mm                                                                                                        | 未实现                                     |
| 提高 4 / p3       | 每次给出信号强度、相关度等质量指标；低质量剔除或明确标注                                                                                                                    | 未实现；已有状态 UI 不等于质量评估         |
| 提高 6 / p4       | 两屏同步刷新；周期单音稳定静止，抖动 ≤±2% 周期；并排零声程差时同相固定；移动一板仅该板相位滑动，仍同步刷新；同步前漂移/跳动、收敛后稳定                                   | 本地波形可用；跨板时间轴、相位与刷新未实现 |
| 可选：提高 5 / p4 | 标定后 0.20–10 m；至 5 m 误差 ≤±5 mm，5–10 m ≤±15 mm                                                                                                                  | 主线完成后再评估                           |

基础 2 原文“相邻两次到达时刻之差不超过 ±100 μs”与循环播放的实际周期存在表述歧义。验收前应向老师确认是否指扣除播放周期后的到达抖动；开发中记录原始时刻、间隔及周期残差，不能把不同次发声的绝对时间强行比较成接近零。

共同约束（p4–5）：仅使用课程提供的板卡/模块/连接线；验收经交换机；距离真值取指定麦克风开孔之间，不是板边；声源在两板连线外侧、距近板 0.5–1 m、尽量共线；使用评委指定设备和标准音频；测量、计算、结果判定在板上完成，串口仅观察和设置参数。报告需有误差预算及实测对照。

## 2. 已完成的工程基础


| 基线 / 模块                     | 已完成                                                                         | 边界或后续工作                                          |
| ------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------- |
| `4980543`、`cc1b82f`、`d95032b` | CubeMX/IAR、BSP 依赖、调试器修正、LCD 网格                                     | 上层 BSP/字体仍需按 README 补齐                         |
| `c58d07f`                       | WM8994/SAI 双麦 PCM16、标称 16 kHz、循环 DMA、本地双通道波形                   | L/R 是一块板上的两颗麦克风，不是 A/B 两板               |
| `475d1f9`                       | 生成 Up–Down–Up 扫频测试 WAV                                                 | 自制音频用于开发；尚未确认与老师标准文件相同            |
| `50c5c4c`（同伴提交）           | 距离仪表 UI、测量状态、毫米结果接口                                            | UI 不计算距离；显示小数位不代表精度                     |
| `d038661`                       | 覆盖扫描、去扫描线、候选测试音触发后默认保持 2 s                               | 开发者确认上板效果；保持不停止 DMA；不是到达时间算法    |
| `be60e1a`                       | TIM5 1 MHz 配置、删除冗余备份                                                  | 旧版本由 Git 保留；同步功能未实现                       |
| `89abe36`                       | CubeMX 分模块代码、LwIP、ETH 初始化修正、LTDC BSP 中断句柄修正、主循环网络处理 | 开发者确认 ping 成功并已上传；尚无双板 UDP/同步验收记录 |

当前网络配置集中在 `Core/Inc/app_board_config.h`：A 为 `192.168.10.10/24`、MAC `02:00:00:00:00:0A`；B 为 `192.168.10.11/24`、MAC `02:00:00:00:00:0B`，源码默认 B。两板 UDP 端口均为 5000，固定互为对端，电脑可用 `.20`。只需切换 `APP_BOARD_ROLE`，IP 与 MAC 自动一起切换；同一网段不能烧录两份同角色固件。

P1 首版已加入 `app_net`：版本化 UDP 消息、HELLO/ACK、每秒心跳及 TEST/ACK、启动会话 ID、3 秒超时及重连。LCD 右上角显示角色、ONLINE/WAIT/ERROR 和成功应答计数。实现与协议见 [双板通信说明](commit_logs/2026-09-18_dual-board-udp.md)。主机协议测试与固件构建不代替实际两板经交换机验收；P0/P1 退出条件仍待上板确认。

当前使用 BSP 精简启动路径，`LCD_GRID_DEMO_ONLY=1`，网口独立初始化，未执行 TIM5 初始化或启动计数。不要把“配置 1 MHz”当作“时钟已运行/同步”。LwIP 堆为 8 KiB，IAR 网络专用链接脚本已保留 RX 池、堆和 ETH 描述符区域；长期网络、音频和显示并行稳定性仍需上板验证。

## 3. 总体技术路线（拟实现）

链路：两板组网 → 同步计时与样点时间映射 → 标准音频检测 → 同事件配对 → 温度补偿/标定/质量判定 → 统一结果发布与同步波形。

### 3.1 通信与同步

优先保持现有无 RTOS 主循环，新增非阻塞状态机，不在网络回调中绘图或长时间计算。先用 UDP 完成身份、心跳和数据交换，再接入同步协议。

优先验证 ETH 硬件时间戳 + 双向四时间戳交换的方案。STM32F746 支持 IEEE 1588v2 硬件，当前 HAL 头文件也有 `HAL_ETH_PTP_*` 接口；这只证明硬件/驱动入口存在，不代表本工程已有 PTP 协议栈。参考 [ST STM32F746 产品说明](https://www.st.com/en/microcontrollers-microprocessors/stm32f746ze.html)。

建议固定 A 为主、B 为从以控制初期复杂度。需实现偏移与频率漂移估计、异常延迟样本剔除、锁定/失锁状态。普通交换机条件下能否达到指标必须实测，不能用低 ping 延迟证明微秒同步。软件收包时间戳可用于早期协议验证，但不作为 ±10 μs 达标保证。

统一时间可采用本地单调计数到主时钟的映射 `T = a*t_local + b`。若使用 ETH PTP 时钟，还必须建立它与 TIM5、音频样点时刻之间的关联；不能直接混用计数值。32 位 1 MHz TIM5 约 71.6 分钟回绕，需设计 64 位扩展/差分及原子读取策略。同步修正应避免正在处理的事件时间轴突然跳变。

整秒脉冲优先由硬件定时比较/输出实现；具体引脚需先核查占用，不预先占用现有音频或 LCD 引脚。禁止用主循环 `HAL_Delay()` 翻 GPIO 来证明微秒级同步。

### 3.2 音频到达时刻与质量

保持每板双麦显示，但先固定一个指定麦克风作为测距参考点，两板选相应通道并在开孔位置标记。不要随意平均 L/R 到达时间改变几何测量点。

为连续音频建立 64 位样点序号及时间锚点，显式记录丢样/溢出。拟采用 `T(n)=T0+(n-n0)/Fs_est` 映射；`Fs_est` 为估计的实际采样率，不永远假定两板恰好都是 16000 Hz。DMA 回调进入时间含中断延迟，不能直接作为某样点采样时刻，应验证锚点误差和音频链路固定延迟。

检测流程建议：去直流/合理滤波 → 能量候选窗 → 标准模板归一化相关 → 签名时序检查 → 峰附近亚采样插值/细化 → 质量判定。现有过零/幅度候选检测继续用于显示调试，不作为精密测距结果来源。模板参数、相位、渐变窗及版本必须与最终验收音频一致；拿到老师文件后先确认特征，必要时更换检测器。

质量至少包括相关峰值、峰旁比或次峰比、背景噪声估计、削顶/丢样标记、同步状态；阈值由实验标定，不凭经验宣称达标。优先考虑直达声，不能简单把最强反射峰当到达时刻。

16 kHz 样点间隔 62.5 μs，以 340 m/s 换算约 21.25 mm/样点；2 mm 对应约 5.88 μs，5 mm 约 14.71 μs。因此整数样点差不足以直接满足目标，应先评估亚采样估计与实际噪声/多径误差；若仍不足，再评估提升采样率及 CPU/内存代价。提高采样率本身也不保证精度。

#### 采样率升级计划（已获开发者确认，尚未修改固件）

本地 BSP 的 `BSP_AUDIO_IN_Init(AudioFreq, ...)` 接收采样率参数，WM8994 驱动包含 32/48/96 kHz 配置分支。后续优先上板验证 **48 kHz、16-bit、双通道**，保留 16 kHz 基线作比较；96 kHz 仅在麦克风/编解码器时钟约束及资源评估通过后考虑，驱动存在分支不等于整条板载采集链路已验证。

以340 m/s换算，48 kHz样点间隔约20.83 μs、声程约7.08 mm；96 kHz约10.42 μs、3.54 mm。它们不是实际测距精度，48 kHz仍需亚采样估计以满足2 mm重复性目标。提高采样率不会增加原始16 kHz播放文件已有的信号带宽，但可细化接收时间网格。

安排在P3前半段完成采样率对比实验，不只修改 `SAMPLE_RATE`：

- 将窗口和检测时序改为由采样率、毫秒数派生；当前 `mic_scope_trigger.h` 有2560帧固定条件，不能原样复用到48 kHz。
- 若保持160 ms显示窗、16 ms DMA半缓冲，48 kHz分别需7680帧、768帧；双通道PCM16完整DMA缓冲为6144字节。所有历史/快照/保持缓冲同步重算并核查内存布局。
- 32 ms chirp、8 ms间隔、1 ms渐变在48 kHz采集模板中分别为1536、384、48样点；模板须按实际播放信号生成或合理重采样，不能把原512点模板直接当48 kHz模板。
- 同步调整相关计算量、过零频率阈值、时间轴、样点时间映射和实际采样率估计；检查SAI/WM8994时钟与CubeMX/BSP初始化覆盖。
- 比较16/48 kHz下丢样率、主循环最大耗时、网络稳定性、检测抖动和测距误差。通过后再将48 kHz作为默认并提交迁移日志，失败则保留16 kHz基线排查。

### 3.3 测距、方向、温度与标定

定义 `delta_t = T_B - T_A`，经链路差分延迟标定后得到 `delta_t_corrected`。共线且声源在线段外侧时，`D = c(T)*abs(delta_t_corrected)`；正值表示 A 先收到、声源在 A 侧，负值表示 B 侧。接近不确定度范围时报告方向不确定，不猜测。声音位于两板之间或明显偏离共线条件时，该公式不再直接给出板间距离。

现场通过串口参数或板上交互设置温度，两板使用同一温度版本，LCD 显示温度和声速。选定并记录适用环境的声速近似模型及有效温度范围，验收测试改变温度后的距离变化；不把 MCU 内部芯片温度直接当空气温度。

单点标定先解决稳定的差分固定偏置：已知距离、温度和声源侧，重复采集高质量事件估计偏置，保存版本及校验信息。一个已知距离不能同时独立确定任意增益和偏置；采样频率/声速尺度应另行确定。标定后移动到多个距离验证，不能在每个点重新标定来代替“一次标定”。换边测试验证符号和偏置模型。

一板计算并向另一板发布同一结果对象，包含会话/事件 ID、距离、方向、质量、温度/标定版本及计划显示时刻；ACK/重试限时，重复消息不能被当作新测量。周期声源的配对必须依据统一时间窗和特征，不只使用各板独立递增的序号，避免漏检后错配。

### 3.4 同步波形不是两块屏各自触发

保留当前覆盖扫描作为调试模式；新增同步观测模式，两个模式明确区分。当前 2 s 保持仅影响调试波形，不得暂停网络、同步、采集或测距结果发布；同步波形验收时关闭该保持。

两板按统一时基取相同起止时间的本地音频窗口，并约定未来刷新时刻；必要时按实际采样时钟映射重采样到共同显示网格。单音稳定显示采用共同相位参考/统一触发纪元，不能各板分别锁自己的过零点，否则移动板时真实相位差会被消掉。不能用复制远端波形伪装本地同相。

需新增适合单音的短时窗（显示几个周期），现有 160 ms 全景不足以直接观察高频单音相位。记录参考频率、显示时间轴与相位抖动统计；同时刷新、采样对齐和信号相位是三个不同的验收点。LCD 垂直消隐与两板刷新调度差也要实测。

## 4. 开发阶段与退出条件

以下顺序为主线；每阶段完成后按 README 写日志并留存数据，再推进下一阶段。先完成基础闭环，再冲提高精度，避免只完善界面。


| 阶段            | 工作与交付                                                                 | 退出条件                                                                       |
| --------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| P0 固化基线     | 两板角色/IP/MAC配置；记录网络拓扑；音频、LCD、ping回归；核查内存和外设归属 | 两板同时联网不冲突，长时间运行无音频停滞；记录测试时长和丢包                   |
| P1 双板业务通信 | UDP版本化消息、HELLO/心跳、序号、会话ID、超时与重连                        | 经交换机自动发现指定对端；断线/重启自动恢复；未知版本拒绝                      |
| P2 时间同步     | 硬件时间戳链路、偏移/频漂估计、锁定状态、GPIO秒脉冲                        | ≤30 s收敛；示波器连续10次偏差≤±10 μs；失锁可观察，恢复可复测               |
| P3 精确到达检测 | 样点时戳映射、标准音模板、亚采样估计、质量字段、丢样检测                   | 本地时刻可输出；基础2歧义确认后按统一口径验收；记录检测抖动                    |
| P4 基础闭环     | 同事件配对、距离/A-B侧、现场温度与声速、两屏一致结果、异常恢复             | 全部基础指标通过：2 s内出结果、0.2–2 m误差/重复性、60 s静默、换设备、温度变化 |
| P5 提高2/4      | 单点标定、参数保存、质量阈值及低质剔除                                     | 一次标定后0.2–5 m各点≤±5 mm；每次结果附质量/拒绝原因                        |
| P6 提高6        | 统一显示时间窗/刷新纪元、单音模式、同步前后演示                            | 按五项同步波形要求逐条验收，单音抖动≤±2%周期                                 |
| P7 整体验收     | 无电脑辅助演示、冷启动/断线/复位回归、报告与误差预算                       | 可复现实验记录、原始数据、两板版本与参数完整                                   |
| P8 可选         | 10 m扩展、复杂方向/角度估计                                                | 不影响P0–P7；单独分支、单独指标和日志                                         |

P3 的模板实验可在 P2 同步调试期间独立推进；P6 的界面可先设计，但最终依赖 P2/P3 的统一样点时间。提高4的质量字段应从P3开始设计，不必等P5才加。

## 5. 接口约定

已新增 `app_net` 和 `app_board_config`，复用现有 `app_mic_scope` 显示网络状态。后续拟新增模块：`app_time_sync`、`app_audio_event`、`app_ranging`、`app_calibration`。优先小模块增量集成，不为拆分而大改已验证驱动。

先冻结数据契约：协议版本、字节序、板ID/会话ID/序号；时间使用明确单位的64位值；事件含样点位置、统一到达时间、同步代次、质量与错误标志；结果含整数毫米、方向、温度/标定版本、有效期。不要直接发送编译器原生结构体而不定义对齐和编码。

原有 `MicScope_SetDistanceMm()` / `MicScope_SetRangeState()` 继续由主循环调用；方向、温度、同步状态和质量需新接口，不假装现有接口已支持。波形保持不能刷新旧结果的有效期。掉线、失锁、丢样、低相关、事件不匹配需有明确原因码。

每次提交前在 `commit_logs/` 写：目的、改动、接口/单位、构建、硬件实测、未验证事项、下一步；同步更新本计划状态。个人 `EWARM/settings/`、临时文件和构建产物不随功能提交。保存原始验收数据，不只写“正常”。

## 6. 验收记录与主要风险

建议每组实验记录：两板固件哈希、角色/IP/MAC、交换机与拓扑、音频文件版本、播放设备/音量、温度/声速、指定麦克风、卷尺真值、源位置、同步偏差、每次到达时刻/样点、相关质量、原始/标定后距离、方向、有效/拒绝原因。

至少覆盖 0.20/0.50/1.00/2.00 m，标定后增加中间点和 5.00 m；每点至少10次，计算单次最大绝对误差、均值偏差、标准差、漏检/误检率。该距离点集合是组内建议，不能替代老师现场任意摆放。另做60 s静默、换播放设备、换源侧、断网重连、播放起始干扰、温度参数变更和同步波形移动实验。

主要风险及处理：

- 同步达到±10 μs不代表重复性≤2 mm：按340 m/s换算10 μs已约3.4 mm，必须把同步残差与检测抖动共同纳入误差预算，内部目标应更严格。
- 单点标定只能去除所选模型的稳定误差，不能消除随机抖动、频漂、多径或错误事件配对。
- 教师标准音频尚未核实：当前自制扫频只作为开发输入，不能当作验收音频已经确认。
- 对称网络时延是假设而非保证；经交换机的排队/路径不对称需通过示波器和负载实验评估。
- CPU忙于LCD/相关计算可能拖慢无RTOS网络处理：记录主循环最大耗时、DMA丢样及网络丢包，分片处理耗时任务。
- 固定地址内存与链接器布局、Cache/MPU、BSP/CubeMX句柄冲突须做回归；ping通过不能覆盖这些风险。

下一步先上板验收已加入的 A/B 身份配置、双板 UDP 心跳与重连，留存 P0/P1 测试记录；之后实现 P2，并以秒脉冲实测作为进入测距集成的门槛。
